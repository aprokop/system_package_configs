#!/bin/bash
./bootstrap.sh --prefix=/home/aprokop/local/opt/boost-1.53.0 --show-libraries

# Enable MPI (otherwise mpi module is not built)
echo "using mpi ;" > ~/user-config.jam

# GCC
/b2 install

# CLANG
#./b2 install toolset="clang"
